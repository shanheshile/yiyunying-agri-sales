# Claude Project Entry

Generate the current task prompt with `python scripts/build_prompt_pack.py` and
use it as project instructions. Load only one variant and the required task
modules. External actions remain draft-only without a current authorization.
