{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Credential Bindings",
  "type": "object",
  "required": ["schemaVersion", "services"],
  "properties": {
    "schemaVersion": { "type": "string" },
    "secretProviderOrder": {
      "type": "array",
      "items": { "enum": ["browser-session", "environment", "os-credential-manager", "platform-credential-store", "desktop-session"] }
    },
    "services": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["authMode"],
        "properties": {
          "url": { "type": "string" },
          "authMode": { "type": "string" },
          "usernameRef": { "type": ["string", "null"], "pattern": "^(env|credential-manager|platform):" },
          "passwordRef": { "type": ["string", "null"], "pattern": "^(env|credential-manager|platform):" },
          "tokenRef": { "type": ["string", "null"], "pattern": "^(env|credential-manager|platform):" }
        },
        "additionalProperties": true
      }
    }
  },
  "additionalProperties": false
}
